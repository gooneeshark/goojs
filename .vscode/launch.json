{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Goohub - Launch Local",
            "request": "launch",
            "type": "msedge",
            "file": "${workspaceFolder}/index.html",
            "webRoot": "${workspaceFolder}"
        }
    ]
}